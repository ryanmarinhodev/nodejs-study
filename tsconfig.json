{
  "compilerOptions": {
    "target": "es2016",                                  /* Define a versão do JavaScript emitido. */
    "jsx": "react-jsx",                                  /* Configuração essencial para suportar JSX em arquivos .tsx. */
    "module": "commonjs",                                /* Módulo comum usado em projetos Node.js e React. */
    "rootDir": "./src",                                  /* Define a pasta de origem do projeto. */
    "moduleResolution": "node",                          /* Resolução de módulos padrão do Node.js. */
    "outDir": "./dist",                                  /* Diretório de saída para os arquivos compilados. */
    "esModuleInterop": true,                             /* Facilita a importação de módulos CommonJS. */
    "forceConsistentCasingInFileNames": true,            /* Garante consistência de uso de maiúsculas/minúsculas em imports. */
    "strict": true,                                      /* Ativa checagem estrita de tipos. */
    "skipLibCheck": true                                 /* Ignora a checagem de bibliotecas para melhorar a performance. */
  },
  "include": ["src"],                                    /* Inclui a pasta src, onde seus arquivos .tsx estão localizados. */
  "exclude": ["node_modules"]                            /* Exclui a pasta node_modules para evitar checagem desnecessária. */
}
